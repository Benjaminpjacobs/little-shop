.user-dashboard
  %h2
    =current_user.full_name
    's Dashboard
  %section
    %p=current_user.email
    %p=current_user.full_address unless current_user.addresses.empty?
    %p=link_to "View Orders", user_orders_path(current_user)