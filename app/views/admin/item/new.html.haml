%h1 Make a New Item

%p Please complete the given fields for a new item

=form_for(@item) do |f|

  =f.label :name
  =f.text_field :name

  =f.label :description
  =f.text_field :description

  =f.label :price
  =f.text_field :price

  =f.label :category
  =f.collection_select :category_ids, Category.order(:title),:id,:title, include_blank: true

  =f.submit "Create Item"
