%h1.page-heading=@category.title

.item-wrapper
  -@category.items.each do |item|
    .item-preview
      %div=image_tag(item.image)
      %h3=item.name
      %p=item.description
      %p=item.price
      =button_to "Add to Cart", carts_path(item_id: item.id)
